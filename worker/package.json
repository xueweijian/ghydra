{
  "name": "ghydra-worker",
  "version": "0.5.0",
  "private": true,
  "description": "GHydra B-channel Cloudflare Worker: GitHub reverse proxy with whitelist, streaming and conservative caching (gh-proxy protocol compatible)",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "dry-run": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.0.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "wrangler": "^4.0.0"
  }
}
