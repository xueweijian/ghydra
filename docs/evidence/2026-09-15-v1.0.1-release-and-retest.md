# v1.0.1 发布与复测报告

- 日期：2026-09-15 08:00–08:15（Codex 执行）
- 版本：v1.0.1 = F11（发布链前端）+ F13（GUI 连接链）+ F1（NSIS PATH）+ F9/F6/F7（速赢包）

## 发布链

| 环节 | 结果 |
|---|---|
| main 合入（PR #3/#4/#5，全绿） | F13 → F9/F6/F7 → F1 三批合入，线性历史 |
| tag v1.0.1 → release.yml | frontend job + 三平台 + production 标签首跑 ✅ 8 产物 + checksums |
| 产物抽验 | gui zip SHA256 与 checksums 一致；二进制含 `assets/index-B32rbZbg.js`（前端在）；体积较 rc1 减 ~170KB（production 标签去除 dev runtime） |
| sign-release | `~/ghydra-keys/ghydra-release.key`（minisign -W，158B 校验 + 与冻结公钥往返签名验证双过）→ checksums.txt.minisig 上传 → 已发布 ✅ |

## 真机装机与 GUI 验收（正式产物）

- v1.0.0 setup 装回 → `update check` 发现 v1.0.1 + 正确选 gui 变体资产 ✅
- 正式 v1.0.1 setup 静默装：`ghydra version 1.0.1` ✅、HKLM PATH 含 GHydra ✅、App Paths 键 ✅（F1）
- `ghydra gui`：`[inject] SetURL 已应用连接（port=9801）`，面板「daemon 已连接 · v1.0.1」、连接表/doctor 快照实时出数（F13 首次在正式产物上全链生效）

## 复测协议（v2ray 全关，真实直连网络）

| 步骤 | 结果 |
|---|---|
| doctor both | direct 0/5（12s）——本轮窗口 github.com 域级阻断（分钟级波动，四轮报告同款环境态）；proxy 0/5/1ms（serve 未跑，proxy_unreachable 语义正确） |
| bench（F6 默认 direct） | **5/6**；dst_ip 全出数（F9）：api→20.205.243.168、codeload→.165、avatars/objects→185.199.110.133、raw→185.199.109.133；github.com timeout |
| on 接管 + 冷启动 ×5 | **5×HTTP 000 @ 7.00s**——A 通道选中未预筛死 IP（192.30.252.2 类，meta 种子初始分虚高）单发即败；域级阻断 + 无镜像兜底（F2） |
| warm ×3（raw 域） | **3×200 @ 0.29–0.54s** ✅ |
| gh-proxy 探活 | HTTP 403（持续死，F2 实证续） |
| off + 残留 | 注册表还原快照 ✅、进程 0、9801 无监听 ✅ |

## 自更新（v1.0.0 → v1.0.1）专项实证 → F2/F3 排期依据

- `update check` ✅（api.github.com A 通道小请求可过）
- `update`（apply）**失败 ×3**：checksums 下载 A 通道死 IP 10s timeout / B 通道 gh-proxy 403；curl 直连同 URL 200（系统 DNS 选到好 IP）——**根因 = get 一次性进程的池预筛不完整 + meta 种子未预筛初始分虚高，每次确定性选中死 IP**。与 v2ray 开关无关（ghydra 下载不走系统代理）。
- 附带小瑕疵（记遗留）：提权更新残留 staging 后，非提权进程 `清扫 staging unlinkat Access denied` 每次命令都刷一行；release notes 仍是 draft 模板文案（sign-release 未生成正式 notes）。

## 结论

1. **v1.0.1 发布链、安装器、GUI 面板全链验收通过**——四轮验收的 9 项异常中 F1/F6/F7/F9/F11/F13 六项闭环。
2. 剩余体验短板全部有实证数据支撑排期：冷启动 7s×5 超时与自更新下载失败 → **F2（镜像降级链）+ F3（拨号帽/竞速）+ F8（池加厚）为 v1.0.2/v1.1.0 最高优先**。
3. F12（off×supervisor）未在本轮复现窗口（未跑 GUI 常驻场景），留 v1.0.2。
