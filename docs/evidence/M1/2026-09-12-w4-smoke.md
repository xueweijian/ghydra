# M1 W4 真网冒烟（2026-09-12）

网络状态：用户确认已关闭魔法；本轮沙箱直连测试未使用 `HTTP(S)_PROXY` 环境变量。

## doctor direct

- 5 个覆盖场景：5/5
- SSH 观测：2/2（22、`ssh.github.com:443` TCP 可达）
- web/login/clone/push/release：HTTP 200/200/200/401/200
- Release TTFB 约 1.5s

## doctor proxy

- 本地 GHydra CONNECT：`127.0.0.1:19801`
- 5 个覆盖场景：5/5
- SSH 观测：2/2
- web/login/clone/push/release：HTTP 200/200/200/401/200
- 调度器最终择优：`20.205.243.166:443`，约 70ms；老 `192.30.x` 候选被 Preflight 熔断

## 结论

W4 探针、直连对照、代理通道、池预筛和根因分类均完成机制验证。以上 RTT/状态只代表本次网络窗口，不作为 7 天 SLA 数据；Windows 主力机 `ghydra on/off` + 注册表 PAC 真机验收仍是 dogfooding 入口。


